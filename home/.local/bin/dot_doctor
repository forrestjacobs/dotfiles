#!/usr/bin/env bash
set -euo pipefail

for script_path in "${HOME}"/.config/dotfiles/setup/*.sh; do
  script_name=$(basename -- "$script_path" .sh)
  if ! dot_setup check "${script_name}"; then
    echo "Call 'dot_setup ${script_name}' to set up $(dot_setup describe "${script_name}")"
  fi
done
