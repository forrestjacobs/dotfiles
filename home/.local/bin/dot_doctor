#!/usr/bin/env bash
set -euo pipefail

setups=( "brew" "gitconfig" "gh" "sudo" "fish" )

for name in "${setups[@]}"; do
  cmd="dot_setup_${name}"
  if ! "${cmd}" check; then
    echo "Call '${cmd}' to set up $("${cmd}" description)"
  fi
done
