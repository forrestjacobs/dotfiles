is_me = /forrest|jacobs/i.match? ENV["USER"]

brew "bat" if OS.mac?
brew "eza"
brew "fish" if OS.mac?
cask "font-monaspace" if OS.mac?
brew "gh" if is_me
brew "gitui" if is_me
brew "helix"
brew "htop" if is_me
brew "patchutils" if OS.mac?
brew "stow" if OS.mac?
brew "wget" if is_me
brew "zoxide" if is_me
