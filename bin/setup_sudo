#!/usr/bin/env bash
set -e

for target_group in sudo admin wheel; do
  if getent group "$target_group" > /dev/null; then
    echo "%$target_group ALL=(ALL:ALL) NOPASSWD: ALL" | sudo tee "/etc/sudoers.d/50-nopasswd"
    exit
  fi
done

echo "could not determine sudo group"
exit 1
